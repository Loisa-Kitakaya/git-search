import { RepositoryInformation } from './repository-information';

describe('RepositoryInformation', () => {
  it('should create an instance', () => {
    expect(new RepositoryInformation()).toBeTruthy();
  });
});
